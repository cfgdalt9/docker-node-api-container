# Docker Node API Container

Simple Dockerized Node.js API application.

## Setup
```bash
docker build -t node-api .
```

## Run
```bash
docker run -p 3000:3000 node-api
```

## Build
docker build -t node-api .